{
    "name": "tabnabbing-watch-tests",
    "private": true,
    "scripts": {
      "test:serve": "python3 -m http.server 8000",
      "test:e2e": "playwright test -c tests/playwright.config.ts",
      "test:install": "npx playwright install chromium"
    },
    "devDependencies": {
      "@playwright/test": "^1.49.0"
    }
  }